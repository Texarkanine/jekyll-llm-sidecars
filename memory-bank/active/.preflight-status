PASS WITH ADVISORY

Wrong-reference, prerequisites, TDD encoding, convention, dependency, conflict, and completeness checks passed. Complexity is Level 2. Planning checkboxes are complete. No creative docs are owed. Units are labeled executable but they only rewrite specs with no lib/ change, so they are not executable under What TDD Governs; they still list test steps before any deletion or hook edit. No change-detector steps to strike. spec/body_spec.rb matches one-file-per-subject. The eight Body examples already call Body.call through build_site and do not depend on generator_spec lets. scope_builders is a mutable array with replace. All 15 audit findings map to a unit. The four strengthened Body oracles are already true: unknown-filter Body.call returns "x\n", unknown-variable Body.call returns "\n". Derived Markdown count on the shared files fixture is 7.

Unit 3's revised oracle holds on the success path without profile: true. After Body.call on marked.md, site.liquid_renderer.stats_table is a Filename/Count/Bytes/Time table whose first data row is marked.md (count 1). page.path is "marked.md". Jekyll 4.4.1 LiquidRenderer#file always inserts the normalized path into @stats; profile only affects timing display.

Unit 6's revised oracle holds. The sidecar is a figure.highlight / code.language-ruby fragment whose tokens are <span class="nb">puts</span> <span class="ss">:hi</span>. The raw {% highlight tag is gone. After stripping tags the text is "puts :hi\n".

Unit 1's around do ... ensure ... end form is valid on this Ruby. The brace form remains a SyntaxError; the plan already forbids it.

Advisory / radical innovation: fold the new "records the document path on the liquid renderer" example into unit 2 so every Body oracle lands in one unit, and build that page at docs/marked.md. On a root marked.md, page.path equals page.name, so stats_table.include?(page.path) stays green if render_liquid is mutated to pass item.name. A nested path makes include?(page.path) false for that mutant. Unit 3 then only deletes the @stats read from the generator example. Do not make this change in preflight.
