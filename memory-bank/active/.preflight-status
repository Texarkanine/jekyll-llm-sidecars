PASS

## Verification

Re-read the revised `memory-bank/active/tasks.md` against the live codebase and against the prior `FAIL (fixable)` findings.

- **Prior finding 1 (exclude default regression) — fixed.** Unit 1 step 4 now migrates `Configuration::DEFAULT_EXCLUDE` from `/assets/**` to `/assets/**/*` in `lib/jekyll/llms_txt/configuration.rb`, `README.md`, and `spec/configuration_spec.rb`'s `default_exclude` let, before the `FNM_PATHNAME` flag change lands. Verified `File.fnmatch?("/assets/**/*", "/assets/nested/note.md", File::FNM_EXTGLOB | File::FNM_PATHNAME)` is `true`, so `spec/census_spec.rb:56`'s existing assertion stays correct.
- **Prior finding 2 (stale seven-body corpus example) — fixed.** Unit 2 step 1 now names `spec/generator_spec.rb:162` ("writes sidecars and joins corpus bodies with one newline") explicitly in the stub-tests step, alongside the two short join examples, and renames it away from "one newline".
- **TDD Plan Encoding** — each of the three units places stub-tests, stub-interface, red, then green in that order; no unit skips ahead to code. No change-detector or contract-test steps found to strike.
- **Convention Compliance** — proposed files (`lib/jekyll/llms_txt/census.rb`, `generator.rb`, matching specs, `README.md`) match the existing module layout; no new classes or naming deviations.
- **Dependency Impact** — read `Census`, `Generator`, `Manifest`, `Scope`, `ScopeBuilder`, `Entry`, `Summary`. `Manifest::Row#scope` is populated for every `:corpus` row (never `nil`), so unit 3's `row.scope.path_prefix == "/"` branch in `render_row` has a scope to read. `spec/manifest_spec.rb` and `spec/llms_spec.rb` operate at a level unaffected by the corpus-text change. No category/collection `llms-full.txt` text is asserted anywhere today, so unit 3 adds coverage without colliding with an existing example.
- **Conflict Detection** — no existing helper already renders per-document headings or a two-newline join; nothing in `lib/jekyll/llms/` (an unused parallel tree, not required by any entrypoint) overlaps with this change.
- **Completeness** — all four project-brief requirements map to a plan unit with concrete files and steps; acceptance criteria are each covered by a stubbed test.

## Radical Innovation (advisory, not applied)

`render_row`'s new heading branch (unit 3) reads `row.scope.path_prefix == "/"` — a string-literal comparison against the same `"/"` sentinel that `ScopeBuilder#root_scope` and `Manifest#output_path` already encode positionally. A more accretive design: add a `Scope#root?` predicate (`path_prefix == "/"`) on the `Scope` class itself, and have `render_row` call `row.scope.root?` instead of re-deriving the sentinel. That turns "is this the root scope" from a fact three call sites must each get right into a fact the domain object answers once — and gives the next feature that needs the same distinction (e.g. a future root-only vs. scoped index behavior) one obvious place to ask. Advisory: small enough to fold into unit 3 if the operator likes it, but not required to unblock this plan.
