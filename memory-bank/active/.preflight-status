PASS WITH ADVISORY

Level 2 plan for rename-jekyll-llm-sidecars. Planning is complete. No in-phase plan edits were made.

TDD plan encoding passes. Unit 1 (executable rename) puts spec retargeting and a red run (step 3) before any lib, gemspec, or config change (step 4). The require-path, no-nested-namespace, config-key, and old-key-ignored examples assert behavior a plugin user can observe; none is a change-detector. Unit 2 (README, CONTRIBUTING, memory bank) is prose and owes no tests. The "different config key" example that the plan retargets exists at `spec/configuration_spec.rb:16` (it uses `llms-txt` today).

Convention compliance passes. The entry file and directory keep matching the gem name, and the flat top-level module keeps the existing no-`lib/jekyll/`-tree pattern. `.rubocop.yml` `Naming/FileName` exclusion, `config/mutant.yml` subject, the gemspec `Dir[]` file list, `release-please-config.json`, and both workflow paths are all accounted for. No Bundler frozen or deployment setting blocks `bundle install`.

Dependency impact and completeness pass. A full sweep for the old names (including hidden files, `.github`, `spec/support`, CHANGELOG, and ci.yaml) found no touchpoint missing from the plan. The README rewrite named in the brief is already committed in 590a818, so step 2's rename edits within it satisfy that requirement.

Advisory findings:

1. Step 4's wording "`module JekyllLlmsTxt` → `module JekyllLlmSidecars` in every lib file" covers only module declarations. Non-declaration references also need to change: `JekyllLlmsTxt.scope_builders` (scope_builder.rb:97), `JekyllLlmsTxt.current_destinations` (generator.rb:37, hooks.rb:27), and `JekyllLlmsTxt::Hooks.*` in the top-level hook registrations (hooks.rb:54-62). The red suite and the final sweep will catch them, but replace every `JekyllLlmsTxt` in lib at once.
2. The final sweep regex `jekyll-llms-txt|JekyllLlmsTxt|"llms_txt"|llms_txt:` does not match several things the plan edits: `@llms_txt` (generator.rb, hooks.rb), the backticked `` `llms_txt` `` in the configuration.rb doc comment, `Jekyll::LlmsTxt`, and `jekyll/llms_txt`. Add these alternations so the sweep verifies them.
3. Several SumMem notes name `JekyllLlmsTxt`, `lib/jekyll-llms-txt/`, `jekyll/llms_txt`, and the `llms_txt` config block. Those notes go stale after the rename. SumMem forbids rewriting notes, so at build time record one `.summem/summem note` saying the gem, module, require path, and config block key are now `jekyll-llm-sidecars` / `JekyllLlmSidecars` / `llm_sidecars`.
4. For QA, not this repo: devblog needs its Gemfile entry, its `_config.yml` block key, and its `_plugins` scope-builder module reference changed.

Innovation advisory: the clean break has one known consumer, devblog. A site that still has an `llms_txt:` block and no `llm_sidecars:` block gets the defaults with no error. For example, `create_llms_full` goes back to off and nobody is told. `Configuration#initialize` could log `Jekyll.logger.warn "LLM Sidecars:", "ignoring legacy llms_txt config block; rename it to llm_sidecars"` when `site.config.key?("llms_txt") && !site.config.key?("llm_sidecars")`. That is one guard plus one spec (using `expect(Jekyll.logger).to receive(:warn)`). It fits with the planned old-key-ignored example, which still asserts that defaults apply. This is still a clean break, because the old key is never honored; the only change is that the silent fallback becomes a visible one. Cost is roughly five lines. Decide whether it is worth the permanent reference to a name that was never published.
