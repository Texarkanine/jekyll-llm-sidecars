FAIL (fixable)

Wrong-reference, prerequisites, TDD encoding, convention, dependency, and conflict checks passed. Complexity is Level 2. Planning checkboxes are complete. No creative docs are owed. Units are labeled executable but they only rewrite specs with no lib/ change, so they are not executable under What TDD Governs; they still list test steps before any deletion or hook edit. No change-detector steps to strike. spec/body_spec.rb matches one-file-per-subject. The eight Body examples already call Body.call through build_site and do not depend on generator_spec lets. scope_builders is a mutable array with replace. All 15 audit findings map to a unit. The four strengthened Body oracles are already true: unknown-filter Body.call returns "x\n", unknown-variable Body.call returns "\n". Derived Markdown count on the shared files fixture is 7.

Fixable: Unit 3 schedules expect Liquid::UndefinedVariable whose message includes the page source path. Against Jekyll 4.4.1 and Liquid 4.0.4 that message is "Liquid error (line 1): undefined variable nosuch". template_name is nil because LiquidRenderer::File#parse never passes the filename into Liquid::Template.parse, and Renderer#render_liquid logs format_error(e, path) then re-raises e. The path is not on the exception. The same Body.call already writes a stats_table row for marked.md (count 1), including when render raises. Unit 3 step 4 forbids a lib/ change, so the scheduled example cannot go green. Known fix: make the unit assert site.liquid_renderer.stats_table includes page.path. profile: true is not required; file(path) records the name on every call.

Fixable: Unit 6 schedules include("puts :hi") on the sidecar. The rendered fragment is a figure.highlight / code.language-ruby wrapper whose tokens are <span class="nb">puts</span> <span class="ss">:hi</span>. The raw tag is gone and the two class names are present; the contiguous substring puts :hi is not. Known fix: put the Challenges strip-tags oracle in the numbered step.

Advisory: the audit's around { ... ensure ... } brace form is a SyntaxError. around do ... ensure ... end is valid on this Ruby.

Advisory / radical innovation: drop the Liquid-error example. Add one Body example that calls Body.call and expects stats_table to include page.path. That is the public wrapper around the @stats keys the current test reads. Fold it into unit 2 so path attribution moves with the Body suite; unit 3 then only deletes the ivar read.
