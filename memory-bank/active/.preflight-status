PASS WITH ADVISORY

Preflight run 2026-09-24 against memory-bank/active/tasks.md (Level 2, fix-review-crashes).

Wrong-reference guard: progress.md declares Level 2, so the default (L2/L3) checks are the correct reference. Pass.

Prerequisites: tasks.md marks initialization, test planning, implementation plan, technology validation, and pre-mortem complete, with files, functions, and approaches identified per unit. No creative phase required at Level 2. Pass.

TDD plan encoding: units 1-3 (scope sort, nil categories, extensionless sidecar) each order stub tests, red tests, then green code explicitly enough that coding first is not a reasonable reading. Red expectations name the real failure (NoMethodError from nil comparison, ArgumentError from uniq.sort, unchanged /about row). Unit 3's README bullet explicitly owes no test, which is correct for prose. Unit 4 (missed names) is prose/policy and owes no tests. No change-detectors or non-contract lockstep tests are scheduled, so no strike was needed; no order swap was needed. Pass, no edits made to tasks.md.

Convention compliance: touched files (lib/jekyll-llm-sidecars/scope.rb, scope_builder.rb, manifest.rb; spec/scope_spec.rb, spec/manifest_spec.rb) match existing layout, and the plan edits existing methods rather than adding modules. Pass.

Dependency impact: sort_entries is only reached via Scope#initialize; category_names feeds category_names_in_census and the category select, and the plan re-runs the neighboring category examples; sidecar_path is only reached via add_sidecars, and the plan re-runs the sidecar examples. No unaccounted downstream consumers. Pass.

Conflict detection: the fixes use stdlib (to_time, Array, string append) with no overlapping in-repo utility; no public signatures change; create_llms_txt is explicitly preserved. Pass.

Completeness: requirement 1 maps to unit 1, requirement 2 to unit 2, requirement 3 to unit 3 plus the README bullet, requirement 4 to unit 4's scoped search; all seven listed behaviors have example homes. Pass.

Advisories (non-blocking):
1. Radical-innovation sketch: normalize the date once per entry (precomputed sort key via sort_by, e.g. [time, path] with newest-first ordering) instead of calling to_time inside the sort comparator, so conversion happens O(n) rather than O(n log n) and every future consumer shares one canonical instant. Not a plan defect; evaluate against redesign cost.
2. Acceptance criterion 4 ("no leftover old project name except the directory path itself") overstates the plan's scoped search, which by design excludes memory-bank/archive/, coverage/, create_llms_txt, and the checkout directory. Confirm the exclusions are the intended reading before QA judges criterion 4.
