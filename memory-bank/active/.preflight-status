PASS WITH ADVISORY

Wrong-reference guard: progress.md is Level 2. This is the correct check file.

Prerequisites: planning checkboxes in tasks.md are complete. No creative docs required at Level 2.

TDD Plan Encoding: Unit 1 is executable and lists stub / red / green in that order. Unit 2 is prose/policy (README, CONTRIBUTING) and owes no tests. No scheduled change-detectors. No step swap or strike.

Convention: the planned tree matches the three siblings (one `lib/jekyll-llms-txt.rb`, one `lib/jekyll-llms-txt/` directory, one `module JekyllLlmsTxt`). lib/jekyll/ contains only llms_txt.rb plus llms_txt/, so deleting that directory after the move is correct. Jekyll::Page / Renderer / Utils / Hooks / Generator are already qualified in lib (scope_builder uses ::Jekyll::Utils); flattening does not change those lookups. systemPatterns.md still names Jekyll::LlmsTxt; that is the current briefing, and this task is the change. Update it when the move lands, not in this plan.

Conflict: renaming Jekyll::LlmsTxt is the public change the brief asks for. 0.1.0. ../devblog is already out of scope. Not a conflict.

Dependency / completeness: step 1.4 as written can go green. Every lib file, including version.rb, opens the nested `module Jekyll` / `module LlmsTxt` pair. Qualified `Jekyll::LlmsTxt` remains in hooks.rb (keep_destinations plus the three top-level Jekyll::Hooks.register call sites) and generator.rb (current_destinations). The only inner form is LlmsTxt.scope_builders at scope_builder.rb:98. The gemspec reads Jekyll::LlmsTxt::VERSION from lib/jekyll-llms-txt/version.rb. lib/jekyll-llms-txt.rb already exists and requires jekyll/llms_txt/...; pointing those at jekyll-llms-txt/... after the move is the remaining entry-file edit. No other inner LlmsTxt. site. spec/support does not name the plugin constant.

Advisory / extra end: replacing the nested pair with one `module JekyllLlmsTxt` leaves an extra `end` in each of those files (siblings close the flat module with one). Drop it in the same rewrite. class << self in generator.rb stays on the outer module and becomes JekyllLlmsTxt.current_destinations once the qualified name is replaced.

Advisory / child helper: a global replace of Jekyll::LlmsTxt in spec/ turns defines_llms_txt? into a JekyllLlmsTxt-defined check. That is enough for red (library still defines the nested constant). It does not assert the child's "Jekyll::LlmsTxt is not" half of the behavior list; the in-process example is the suite contract for that. Write the LoadError example as a real raise, as step 1.3 already says. After the replace, write the in-process example so it still says defined?(Jekyll::LlmsTxt), not the new name.

Advisory / radical innovation: do not mv the llms_txt files and then search-replace. Rehome each lib/jekyll/llms_txt/*.rb as a new lib/jekyll-llms-txt/*.rb that opens `module JekyllLlmsTxt` and closes with one `end` (same shape as jekyll-mermaid-prebuild/lib/jekyll-mermaid-prebuild/version.rb). version.rb is already in the destination; rewrite it that way. The require_relative list in lib/jekyll-llms-txt.rb is the checklist of files to rehome. Then delete lib/jekyll/. That makes a leftover nested pair or an extra end structurally the same miss. Do not apply this in preflight; it is for the operator to weigh against the written move-then-rewrite.
