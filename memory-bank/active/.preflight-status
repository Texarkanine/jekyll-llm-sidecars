FAIL (fixable)

Wrong-reference guard: progress.md is Level 2. This is the correct check file.

Prerequisites: planning checkboxes in tasks.md are complete. No creative docs required at Level 2.

TDD Plan Encoding: Unit 1 (constant and require path) is executable and lists stub / red / green in that order. Unit 2 is prose/policy (README, CONTRIBUTING) and owes no tests. No scheduled change-detectors. No step swap or strike.

Convention: the planned tree (lib/jekyll-llms-txt.rb, lib/jekyll-llms-txt/*.rb, module JekyllLlmsTxt) matches the brief. lib/jekyll/ contains only llms_txt.rb plus llms_txt/, so deleting that directory after the move is correct. Jekyll::Page / Renderer / Utils / Hooks / Generator are already qualified in lib (scope_builder uses ::Jekyll::Utils); flattening the module does not change those lookups.

Conflict: renaming Jekyll::LlmsTxt is the public change the brief asks for. 0.1.0. ../devblog is already out of scope. Not a conflict.

Dependency / completeness (fixable): step 1.4 still cannot go green as written. It only replaces the string Jekyll::LlmsTxt. Every lib file opens the module as a nested pair, not that string:

- lib/jekyll-llms-txt/version.rb is already in the destination directory and declares `module Jekyll` / `module LlmsTxt`. It never contains Jekyll::LlmsTxt. After the gemspec line becomes JekyllLlmsTxt::VERSION, `bundle exec rspec` evaluates the gemspec first and NameErrors. This is the inverse of the last fail (qualified names without declarations, instead of declarations without qualified names).
- The same nested pair is how body, census, configuration, entry, generator, hooks, manifest, scope, scope_builder, and summary declare the module. A qualified-name replace leaves those classes on Jekyll::LlmsTxt while specs ask for JekyllLlmsTxt.
- scope_builder.rb:98 is `LlmsTxt.scope_builders` (inner const, not Jekyll::LlmsTxt). A flatten that does not rewrite that line NameErrors in custom_scopes. Known fix: make step 1.4 rewrite all three forms of the same constant — the nested `module Jekyll` / `module LlmsTxt` pair in every lib file including version.rb, every Jekyll::LlmsTxt, and that inner LlmsTxt. — to JekyllLlmsTxt.

Advisory / red-phase prediction: after step 1.3, RSpec.describe JekyllLlmsTxt::… NameErrors while loading spec files because the constant does not exist yet. That is still red, but it is not “the require example fails because the library still defines Jekyll::LlmsTxt.” Write the LoadError example as a real raise (parent-process `expect { require "jekyll/llms_txt" }.to raise_error(LoadError)` or a child that asserts LoadError). Reusing defines_llms_txt? and expecting false passes on red: require "jekyll/llms_txt" still succeeds and defines Jekyll::LlmsTxt, so JekyllLlmsTxt stays undefined.

Advisory / radical innovation: do not describe the green step as a string replace of Jekyll::LlmsTxt. State a constant-identity rewrite of those three forms, and add one in-process example (not a child) that after the suite’s normal load — gemspec plus require "jekyll-llms-txt" — defined?(Jekyll::LlmsTxt) is nil and JekyllLlmsTxt::VERSION is defined. The child require examples cannot see a leftover nested module that Bundler already loaded. That example is the suite contract for AC2; the grep in Challenge 1 becomes backup. Do not apply this in preflight; it is for the operator to weigh against one extra example.
