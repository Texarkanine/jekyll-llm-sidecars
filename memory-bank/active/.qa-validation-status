PASS

## Verification

Reviewed commit `b7e9a1c` ("fix: match ** exclude globs and title corpus documents") against `memory-bank/active/tasks.md` (implementation plan), `memory-bank/active/projectbrief.md` (4 requirements, 3 acceptance criteria), and the live code. Re-ran the suite: `bundle exec rspec` 132 examples, 0 failures, 100% line coverage; `bundle exec rubocop` no offenses.

- **Completeness** — all three plan units landed as specified: `Census#excluded?` matches with `File::FNM_PATHNAME`; `Configuration::DEFAULT_EXCLUDE` moved to `/assets/**/*` with README, spec, and `productContext.md` mirrors updated; root corpus is chomped bodies joined by `"\n\n"`; non-root corpora prefix each body with `"# {entry.summary.name}\n\n"`; sidecars still render the body alone. All four brief requirements and all three acceptance criteria are behaviorally satisfied.
- **KISS / DRY / YAGNI** — no over-engineering: two small private methods (`corpus_text`, `corpus_block`) with no new classes or abstractions; no duplication; no speculative code. The collection-corpora-gain-H1s side effect is plan-blessed, not scope creep.
- **Regression / Integrity** — naming, structure, and error handling follow existing patterns; `scope.path_prefix == "/"` matches the pre-mortem rule; no hardcoded shortcuts, magic numbers, placeholders, or debug artifacts (debris grep hits are pre-existing spec wording and fixture bodies).
- **Documentation** — README output contract and exclude-glob semantics updated; `productContext.md` default-exclude updated.

## Advisories (non-blocking)

- Plan step 1.4 says to pass `File::FNM_EXTGLOB | File::FNM_PATHNAME`; the code passes only `File::FNM_PATHNAME`. Probed all nine brief-relevant glob/path pairs on this Ruby (`FNM_PATHNAME` = 2, `FNM_EXTGLOB` = 16): `PATHNAME` alone and the OR-ed flags agree on every pair, since `EXTGLOB` only affects `{a,b}` patterns. Behaviorally equivalent; consider aligning the code spelling with the plan or noting the equivalence in the plan.
- The test plan names `/tags/`, `/tags/bitcoin/`, and the `/garden/tags/**/*` variants, but the new census specs pin only `/tags/index.html` and `/error/404.html`. Direct `File.fnmatch?` probing confirms the unpinned variants match (and `/garden/tags/**/*` correctly rejects `/tags/`), so behavior is right today — but those variants have no regression pin in the suite.
- Preflight's `Scope#root?` predicate advisory still stands: `render_row` re-derives the `"/"` sentinel instead of asking the domain object. Still non-blocking, as preflight judged.
