usage: opensource

integration:
  name: rspec

includes:
  - lib
  - spec

requires:
  - ./spec/support/mutant_setup.rb

matcher:
  subjects:
    - JekyllLlmsTxt*

mutation:
  # A neutral that runs the generator suite can exceed the 5s default.
  timeout: 30.0
